import utilities from '../helpers/utilities.js';

const printBreadOptions = () => {
    const domString = 'bread';
    utilities.printToDom('bread', domString);
};

export default { printBreadOptions };